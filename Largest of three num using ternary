#include<stdio.h>
void main()
{
    int a,b,c,larg;
    printf("enter  the values of a,b,c:");
    scanf("%d%d%d",&a,&b,&c);
    larg=((a>b)&&(a>c)?a:(b>c?b:c));
    printf("large number is:%d",larg);
}
